# Lab 3.1 Variables and State - Types
# Author Linda Nikolajeva


# int variable = nt. Int, or integer, is a whole number, positive or negative, without decimals, of unlimited length
i = 3

# float variable = is a function or reusable code in the Python programming language 
# that converts values into floating point numbers
# Floating point numbers are decimal values or fractional numbers like 133.5
fl = 3.5 

# boolean = can have only two values - True or False
isa = True

# str = A string is a sequence of one or more characters (letters, numbers, symbols)
# that can be either a constant or a variable
memo = 'how now Brown cow'

# list = Lists are used to store multiple items in a single variable
# e.g. mylist = ["apple", "banana", "cherry"]
lots = ['l', 'o', 't', 's']

print('variable {} is of type:{} and value:{}'.format('i', type(i), i))
print('variable {} is of type:{} and value:{}'.format('fl', type(fl), fl))
print('variable {} is of type:{} and value:{}'.format('isa', type(isa), isa))
print('variable {} is of type:{} and value:{}'.format('memo', type(memo), memo))
print('variable {} is of type:{} and value:{}'.format('lots', type(lots), lots))